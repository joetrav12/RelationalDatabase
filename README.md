# RelationalDatabase
5 relations compiled into a relational database. Project includes answers to several queries and implementation of relational algebra.

To compile, type "cd " into terminal and then drag the project folder ("RelationalDatabase-master") into terminal, then hit return. Then, type "gcc Main.c -o Main" into terminal and hit return.

To execute, type "./Main" into terminal and hit return.
